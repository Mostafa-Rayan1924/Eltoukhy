export type Links = {
  id: number;
  title: string;
  url: string;
  onMobile?: boolean;
};
export type ContactItem = {
  id: number;
  icon: React.ElementType;
  title: string;
  desc: string;
};
export type WhyUsItem = {
  id: number;
  icon: React.ElementType;
  title: string;
  desc: string;
};
export type GoalsItem = {
  id: number;
  img: string;
  title: string;
  desc: string;
};
